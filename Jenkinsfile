pipeline {
    agent any

    stages {
        stage('Test Jenkinsfile') {
            steps {
                bat 'echo Jenkinsfile is working'
                bat 'dir'
            }
        }
    }
}
